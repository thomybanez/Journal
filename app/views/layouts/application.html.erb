<!DOCTYPE html>
<html>
  <head>
    <title>Journal</title>
  </head>
  <body>  
  <%= image_tag asset_path("Journal Logo.png"), size: "200x80" %>
    <div style="margin: 20px">
    
    <% if current_user %>
    <p style = "margin: 0px; color: black; display:flex; justify-content:space-between; width: 220px;">Welcome, <%= current_user.username.upcase %>
    <%= link_to "Logout", logout_path, method: :delete %>
    </p>
    <% end %>
  
    <%= yield %>

  </body>
</html>